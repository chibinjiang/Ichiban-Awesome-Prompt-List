# Model Adaptation

同一 Prompt 在不同模型上的表现可能不同。

## 需要记录

- 模型名称与版本
- 是否使用系统消息
- Temperature / top_p 等参数
- 上下文长度
- 工具是否可用
- 是否使用结构化输出
- 失败案例

## 不要做的事

不要仅凭一次输出就断言：

- 某模型一定更聪明
- 某种 Prompt 对所有模型都有效
- 某个模型完全不会幻觉

## 建议

先保持 Prompt 主体模型无关，再通过单独的适配层记录模型特定约束。
